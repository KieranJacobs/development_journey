version: "3.8"

services:
  project-manager:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./:/app
      - ./uploads:/app/uploads
    environment:
      - FLASK_DEBUG=1
